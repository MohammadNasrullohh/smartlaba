# Flutter and Firebase libraries already ship their own consumer rules.
# Keep this file minimal so R8 can still shrink aggressively.

-keepattributes *Annotation*
-dontwarn org.codehaus.mojo.animal_sniffer.IgnoreJRERequirement
